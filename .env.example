# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/correcion-automatica
# Para MongoDB Atlas usar: mongodb+srv://username:password@cluster.xxxxx.mongodb.net/correcion-automatica

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=tu-secreto-super-seguro-cambiar-en-produccion
JWT_EXPIRES_IN=7d

# n8n Webhooks (del proyecto actual)
N8N_RUBRIC_WEBHOOK_URL=https://tu-servidor.n8n.example/webhook/rubrica
N8N_GRADING_WEBHOOK_URL=https://tu-servidor.n8n.example/webhook/corregir
N8N_SPREADSHEET_WEBHOOK_URL=https://tu-servidor.n8n.example/webhook/spreadsheet

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Encryption Key (32 bytes en hexadecimal = 64 caracteres)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=tu-encryption-key-de-64-caracteres-hex-cambiar-en-produccion
